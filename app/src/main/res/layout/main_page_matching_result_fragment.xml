<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">
    <TextView
        android:id="@+id/matchingFinishText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="깐부 발견!"
        android:textSize="20dp"
        android:textColor="@color/black"
        app:layout_constraintTop_toTopOf="parent" />

    <ScrollView
        android:id="@+id/contentbox1"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/matchingFinishText"
        app:layout_constraintBottom_toTopOf="@+id/findNewFriendBtn">
        <LinearLayout
            android:id="@+id/contentBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:orientation="vertical">
        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/findNewFriendBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@drawable/rounded_signature_purple2"
        android:text="다른지역 깐부찾기"
        android:textSize="20dp"
        android:padding="20dp"
        android:layout_marginTop="5dp"
        android:layout_marginHorizontal="30dp"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@+id/contentbox1"/>


</androidx.constraintlayout.widget.ConstraintLayout>